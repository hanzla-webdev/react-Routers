import { useState } from 'react'
import reactLogo from './assets/react.svg'
import viteLogo from '/vite.svg'
import './App.css'
import Header from './components/Header/Header'

function App() {
  const [count, setCount] = useState(0)

  return (
    <>
      <h1>react routers</h1>

      // actually we dont realy need app to render the these component sinstead
    //  we use router in main.jsx for routing the whole application components yup that write
    // and there as i ahave already preactie things from code witha harry in which ihave learn reouterprovider
    // make path under the function declartion in array ans this objecrs array is store in methid called Browserroute and this will store in a variable.
    </>
  )
}

export default App
